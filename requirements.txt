# FastAPI 및 웹 서버
fastapi>=0.104.0
uvicorn[standard]>=0.24.0

# LangChain 및 AI 모델
langchain>=0.1.0
langchain-anthropic>=0.1.0
langchain-chroma>=0.1.0
langchain-openai>=0.1.0

# 데이터 검증 및 스키마
pydantic>=2.5.0
pydantic-settings>=2.1.0

# 벡터 데이터베이스
chromadb>=0.4.0

# 환경변수 관리
python-dotenv>=1.0.0

# 데이터 처리
pandas>=2.0.0
numpy>=1.24.0

# HTTP 클라이언트 (API 호출용)
httpx>=0.25.0
aiohttp>=3.9.0

# JSON 처리 (성능 향상)
orjson>=3.9.0

# 로깅 및 모니터링
structlog>=23.2.0

# 비동기 처리
asyncio-throttle>=1.0.2

# 텍스트 처리
tiktoken>=0.5.0

# 타입 힌트 지원 (Python 3.8 이하)
typing-extensions>=4.8.0

# 개발 및 테스트 도구 (선택사항)
pytest>=7.4.0
pytest-asyncio>=0.21.0
black>=23.0.0
isort>=5.12.0
flake8>=6.0.0

# 보안
cryptography>=41.0.0

# 파일 처리
openpyxl>=3.1.0
python-multipart>=0.0.6

# 메모리 및 성능 최적화
psutil>=5.9.0

# 날짜 처리
python-dateutil>=2.8.2

# 환경별 추가 패키지
# 운영환경
gunicorn>=21.2.0  # 운영 서버용

# 개발환경
watchfiles>=0.20.0  # 파일 변경 감지 (uvicorn 리로드용)